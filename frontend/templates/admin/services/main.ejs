<div class="row" style="margin-left: 20px">
<h1>Управление сервисами <button type="button" class="btn btn-sm btn-success glyphicon glyphicon-plus service-add" data-id=""></button></h1>
<div class="col-md-6 col-xs-6" style="background: #fff">
    <table class="table table-hover">
        <caption>В базе находится <span class="count_service"></span></caption>
        <thead>
        <tr>
            <th>ID</th>
            <th>Название</th>
            <th>Время тайма</th>
            <th style="text-align: right">Действия</th>
        </tr>
        </thead>
        <tbody id="service-list"></tbody>
    </table>
</div>
<div class="col-md-5 col-xs-5 col-md-offset-1 affix sidebar" id="service-form" style="right: 50px; top: 10px;"></div>
<div class="modalWindow"></div>

<script type="text/template" id="modal-template">
    <div class="modal fade" id="deleteUserModal" tabindex="-1" role="dialog" aria-labelledby="myModalLabel" aria-hidden="true">
        <div class="modal-dialog">
            <div class="modal-content">
                <div class="modal-header">
                    <button type="button" class="close" data-dismiss="modal" aria-label="Close"><span aria-hidden="true">&times;</span></button>
                    <h4 class="modal-title" id="myModalLabel">Удалить сервис?</h4>
                </div>
                <div class="modal-body">
                    Вы действительно хотите удалить сервис?
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-default btn-close" data-dismiss="modal">Я передумал</button>
                    <button type="button" class="btn btn-danger btn-submit" data-id="<%- serviceId %>">Да, хочу</button>
                </div>
            </div>
        </div>
    </div>
</script>

<script type="text/template" id="service-form-template">
    <h2><% if(name=='') { %>Добавление<% } else { %>Редактирование<% } %> сервиса</h2>
    <div class="alert alert-danger form-message" role="alert" style="display:none">Произошла ошибка</div>
    <div class="row form-group">
        <div class="col-md-8">
            <label for="inputName">Название</label>
            <input type="name" class="form-control" id="inputName" placeholder="Введите название" value="<%- name %>">
        </div>
        <div class="col-md-4">
            <label for="inputTimeout">Время тайма</label>
            <input type="timeout" class="form-control" id="inputTimeout" placeholder="Введите время тайма сервиса" value="<%- timeout %>">
        </div>
    </div>
    <div class="row form-group">
        <div class="col-md-12">
            <label for="inputAuthor">Код программы</label>
            <textarea type="text" rows="10" cols="86" id="inputProgram" placeholder="Введите код программы" value="<%- program %>"></textarea>
        </div>
    </div>
    <input type="hidden" id="inputType" value="<%- type %>" />
    <input type="hidden" id="inputId" value="<%- id %>" />
    <button type="button" class="btn btn-success"><% if(type=='add') { %>Добавить<% } else { %>Редактировать<% } %></button>
</script>

<script type="text/template" id="service-template">
    <th scope="row"><%- id %></th>
    <td><%- name %></td>
    <td><%- timeout %></td>
    <td style="text-align: right">
        <button type="button" class="btn btn-info btn-sm edit-service" data-id="<%- id %>"><span class="glyphicon glyphicon-pencil"></span></button>
        <button type="button" class="btn btn-danger btn-sm delete-service" data-id="<%- id %>"><span class="glyphicon glyphicon-remove"></span></button>
    </td>
</script>
</div>  
